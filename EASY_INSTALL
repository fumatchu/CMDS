#Installing
#Install Rocky Minimal
#Make sure you specify the domain name you want to use for name resolution.
#After the GUI install:
#(Just copy and paste the following lines on the Rocky terminal or remote SSH terminal- remote suggested)

cd /root/
dnf -y install wget
wget -O - https://raw.githubusercontent.com/fumatchu/CMDS/main/mig-installer.sh |bash
